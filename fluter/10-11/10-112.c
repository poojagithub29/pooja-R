#include <stdio.h>
int main()
{
    int n1, n2;
    printf("enter first number:");
    scanf("%d", n1);
    printf("enter second number:");
    scanf("%d", n2);
    int a[0];

    for (int i = n1; i <= n2; i++)
    {

        {
            printf("%d\n", a[i]);
        }
    }

    return 0;
}